# ─────────────────────────────────────────────────────────────────
# Copia este archivo a .env.local y rellena con tus valores reales
# Estas mismas variables deben configurarse en Vercel → Settings → Environment Variables
# ─────────────────────────────────────────────────────────────────

# InsForge — URL pública de tu proyecto
NEXT_PUBLIC_INSFORGE_URL=https://TU-PROYECTO.us-east.insforge.app

# InsForge — Anon key (JWT público, prefijo NEXT_PUBLIC para el browser)
NEXT_PUBLIC_INSFORGE_ANON_KEY=eyJhbGci...

# InsForge — Service key (secreta, solo usada en API routes del servidor)
INSFORGE_SERVICE_KEY=ik_xxxxxxxxxxxxxxxxxxxxx

# InsForge — URL de la Edge Function (opcional, se infiere de NEXT_PUBLIC_INSFORGE_URL si no se define)
INSFORGE_EDGE_FUNCTION_URL=https://TU-PROYECTO.us-east.insforge.app/functions/send-emails-worker

# ─────────────────────────────────────────────────────────────────
# SMTP (opcional — se puede configurar desde la interfaz web)
# ─────────────────────────────────────────────────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=xxxx-xxxx-xxxx-xxxx
